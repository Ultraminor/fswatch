# fswatch
A command-line file system watcher utility, for Linux systems. A thin layer atop the inotify API.
`make` creates a binary in this directory, `fswatch`. Run `fswatch` to view command-line options.

Licensed under MIT-Zero
